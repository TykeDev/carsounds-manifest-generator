name: Build Executable
run-name: ${{ github.actor }} is running action!

on:
  push:
    tags:
      - '*'

jobs:
  build:
    name: Create Release
    runs-on: windows-latest
    steps:
      - name: Get the version
        id: get_version
        run: echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
  
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Installing Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: 1.1.12

      - name: Install Dependencies
        run: bun install

      - name: Create Release
        run: bun build ./manifest_script.ts --compile --outfile=manifest_generator_v${{ env.RELEASE_VERSION }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release v${{ env.RELEASE_VERSION }}
          body: Release v${{ env.RELEASE_VERSION }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./manifest_generator_v${{ env.RELEASE_VERSION }}.exe
          asset_name: manifest_generator_v${{ env.RELEASE_VERSION }}.exe
          asset_content_type: application/octet-stream
